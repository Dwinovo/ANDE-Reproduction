run_name: ande_784_14cls
seed: 42
data:
  size: 784
  task: behavior14
  split_ratio: 0.8
  batch_size: 128
  num_workers: 4
  manifest_raw: data/manifest_raw.parquet
  manifest_stats: data/manifest_stats.parquet
model:
  name: ande
  use_se: true
  se_reduction: 16
train:
  epochs: 50
  optimizer: adam
  lr: 0.001
  scheduler: step
  step_size: 10
  gamma: 0.5
  early_stop_patience: 10
